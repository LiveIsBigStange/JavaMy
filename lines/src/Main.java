import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
//        Scanner scanner = new Scanner(System.in);
//        String s = scanner.nextLine();
        //s.length()
        //s.charAt(index) - find the symbol of string
        //s.equals("") - s == ""
        //s.toUpperCase(), s.toLowerCase()
        //char ch = 'a';
        //(int)'2' = 50 by ASCII
        //(char)60 = 'a' by ASCII
        //(char)8 = backspace by ASCII

        //2
//        boolean uPass = false;
//        boolean nPass = false;
//        boolean sPass = false;
//        for (int i = 0; i < s.length(); i++) {
//            if(s.charAt(i) >= (int) 'a' && s.charAt(i) <= (int) 'z'){
//                uPass = true;
//            }
//            if (s.charAt(i) >= (int) 'A' && s.charAt(i) <= (int) 'Z'){
//                nPass = true;
//            }
//            if (s.charAt(i) >= (int) '0' && s.charAt(i) <= (int) '9'){
//                sPass = true;
//            }
//        }
//        System.out.print(uPass && nPass && sPass);

        //4
        int rooms = 0;
        String str = "3-комнатная квартира, 117.2 м², 2/12 этаж, Гагарина — Ескараева";
        for(int i = 0; i < str.length(); i++){
            if(Integer.parseInt() instanceof int)
            str = str.substring(0, );
        }
        System.out.println(Integer.parseInt("3-"));
        System.out.print(str);
    }
}